services:
  myip:
    image: jason5ng32/myip:custom
    environment:
      - ALLOWED_DOMAINS=ip.jasson.cv,www.ip1234.cc,ip1234.cc
    ports:
      - "${HOST_IP}:${WEB_HTTP_PORT}:18966"
    restart: always
    stdin_open: true
    tty: true
    deploy:
      resources:
        limits:
          cpus: "${CPUS}"
          memory: "${MEMORY_LIMIT}"
    labels:
      createdBy: "bt_apps"
    networks:
      - baota_net

networks:
  baota_net:
    external: true